{{#unless isFastBoot}}
  {{#if shouldRender}}
    {{#in-element destinationElement~}}
      {{#if closePopup}}
        {{yield (action closePopup)}}
      {{else}}
        {{yield}}
      {{/if}}
    {{~/in-element}}
  {{/if}}
{{/unless}}
