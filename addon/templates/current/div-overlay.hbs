{{#if shouldRender}}
  {{#ember-wormhole destinationElement=destinationElement~}}
    {{#if closePopup}}
      {{yield (action closePopup)}}
    {{else}}
      {{yield}}
    {{/if}}
  {{~/ember-wormhole}}
{{/if}}